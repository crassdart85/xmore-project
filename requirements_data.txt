# Requirements for xmore_data module.
# Install with: pip install -r requirements_data.txt

# Core dependencies
pandas>=1.5.0
numpy>=1.23.0
python-dotenv>=0.20.0

# Provider libraries (at least one required)
yfinance>=0.2.0

# Optional providers (enable if available in your environment)
# egxpy>=0.1.0  # Primary provider (install if available)
# Tertiary fallback provider
alpha-vantage>=2.3.0

# Caching
joblib>=1.2.0

# Excel export support
openpyxl>=3.1.0

# Type checking (development)
# mypy>=0.990
# pandas-stubs>=1.5.0
